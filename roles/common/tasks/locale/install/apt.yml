# code: language=ansible
---

- name: "Common | Locale | Install | Apt"
  ansible.builtin.apt:
    name: "{{ common.locale.system.packages.apt }}"
    state: present
    update_cache: false
    force_apt_get: true
    cache_valid_time: 3600
  become: true
  tags:
    - common
    - common-locale
